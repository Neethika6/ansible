- name: Conditions
  hosts: local
  connection: local
  vars:
    Number: 5
  tasks:
    #when: Number < 10
    - name: print if the number is less than 10
      ansible.builtin.debug:
        msg: "{{ Number }} is less than 10"
      when: Number < 10